{% extends 'base.html.twig' %}

{% block title %}Formulaire {{ formName }} - {{ parent() }}{% endblock %}

{% block body %}
    <div style="max-width: 60%; margin:auto;margin-top:100px;color:white" >
        <h1>{{ formName }}</h1>
        {{ form(dataForm) }}
    </div>
{% endblock %}
